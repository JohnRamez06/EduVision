# Database migrations README

## XAMPP MySQL setup

1. Start **Apache** and **MySQL** from XAMPP Control Panel.
2. Open `http://localhost/phpmyadmin`.
3. Import this file:

`/home/runner/work/EduVision/EduVision/database/migrations/eduvision_mysql_schema.sql`

This creates the `eduvision` database, schema objects, and seed data.
